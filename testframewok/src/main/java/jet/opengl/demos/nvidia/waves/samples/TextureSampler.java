package jet.opengl.demos.nvidia.waves.samples;

final class TextureSampler {

	int textureID;
	int sampler;
	
	TextureSampler(int textureID, int sampler) {
		this.textureID = textureID;
		this.sampler = sampler;
	}
}
