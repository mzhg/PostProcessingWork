package jet.opengl.renderer.Unreal4.api;

/**
 * Enumeration for currently used translucent lighting volume cascades
 */
public interface ETranslucencyVolumeCascade {
    int
    TVC_Inner = 0,
    TVC_Outer = 1,

    TVC_MAX = 2;
}
